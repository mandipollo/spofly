import { createTheme } from "@mui/material";

const theme = createTheme({
	palette: {
		base: {
			base1: "#101010",
			base2: "#000000",
		},
		gradient: {
			gradient1: "#7C8EA3",
			gardient2: "#4D5965",
		},
	},
});

export default theme;
